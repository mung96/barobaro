export type FirstStep = {
  firstData?: string;
  secondData?: string;
  lastData?: unknown;
};

export type SecondStep = {
  firstData: string;
  secondData?: string;
  lastData?: unknown;
};

export type LastStep = {
  firstData: string;
  secondData: string;
  lastData?: unknown;
};
