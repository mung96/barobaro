export type FirstStep = {
  title?: string;
  rentalStart?: string;
  rentalEnd?: string;
  body?: string;
  pictures?: File[];
  other?: unknown;
};

export type SecondStep = {
  title: string;
  rentalStart: string;
  rentalEnd: string;
  body: string;
  pictures: File[];
  other: unknown;
};

export type LastStep = {
  title: string;
  rentalStart: string;
  rentalEnd: string;
  body: string;
  pictures: File[];
  other: unknown;
};
